<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trendledger.mapper.InterestTopicMapper">

	<select id="findAll" resultType="java.lang.String">
		SELECT topic_code FROM interest_topics WHERE user_id = #{userId} ORDER BY topic_code
	</select>

	<delete id="deleteAll">
		DELETE FROM interest_topics WHERE user_id = #{userId}
	</delete>

	<insert id="insert">
		INSERT INTO interest_topics (user_id, topic_code) VALUES (#{userId}, #{topicCode})
		ON CONFLICT (user_id, topic_code) DO NOTHING
	</insert>

</mapper>
