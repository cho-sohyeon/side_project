<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trendledger.mapper.BudgetGoalMapper">

	<select id="findByYearMonth" resultType="com.trendledger.domain.BudgetGoal">
		SELECT
			goal_id        AS goalId,
			year_month     AS yearMonth,
			target_amount  AS targetAmount
		FROM budget_goals
		WHERE user_id = #{userId} AND year_month = #{yearMonth}
	</select>

	<insert id="upsert">
		INSERT INTO budget_goals (user_id, year_month, target_amount)
		VALUES (#{userId}, #{yearMonth}, #{targetAmount})
		ON CONFLICT (user_id, year_month) DO UPDATE SET target_amount = #{targetAmount}
	</insert>

	<update id="claimOrphaned">
		UPDATE budget_goals SET user_id = #{userId} WHERE user_id IS NULL
	</update>

	<delete id="deleteAllByUser">
		DELETE FROM budget_goals WHERE user_id = #{userId}
	</delete>

</mapper>
